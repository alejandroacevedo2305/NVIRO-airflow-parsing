# Airflow Configuration
AIRFLOW_UID=50000
AIRFLOW_IMAGE_NAME=apache/airflow:2.8.0-python3.11

# Security Keys (CHANGE THESE IN PRODUCTION!)
# Generate Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=81HqDtbqAywKSOumSha3BhWNOdQ26slT6K0YaZeZyPs=
# Generate secret key: python -c "import secrets; print(secrets.token_hex(16))"
AIRFLOW_SECRET_KEY=09c3f6bc5a5e4e8f9d2a1b3c4d5e6f7a

# Admin Credentials (CHANGE THESE IN PRODUCTION!)
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_MAIL_FROM=airflow@example.com
SMTP_USER=
SMTP_PASSWORD=

# Additional Python Requirements (space-separated)
_PIP_ADDITIONAL_REQUIREMENTS=

# AWS Credentials (Optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# Google Cloud Credentials (Optional)
GOOGLE_APPLICATION_CREDENTIALS=

# Azure Credentials (Optional)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
